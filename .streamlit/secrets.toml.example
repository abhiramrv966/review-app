# Copy this file to .streamlit/secrets.toml for local runs, or paste the same
# content into the "Secrets" box of your app on Streamlit Community Cloud.
# Do NOT commit the real secrets.toml (it is gitignored).

GEMINI_API_KEY = "your-google-ai-studio-api-key"
